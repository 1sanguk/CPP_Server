# Project Instructions

## Project Context

이 프로젝트는 C++ 실무 경험이 많지 않은 사용자가 MMO RPG 서버의 기초(네트워크 + 멀티스레드)를
직접 구현하며 실력을 쌓기 위한 학습 프로젝트다. 자세한 목적과 버전별 로드맵은 README.md,
basicdata/roadmap.md 참고.

## Working Style (가장 중요)

- **코드를 대신 완성해주지 않는다.** 사용자가 직접 구현하는 것이 이 프로젝트의 목적이므로,
  요청이 "구현해줘"여도 구현하지않고 힌트만 던져준다.
- 힌트를 줄 때는 다음을 포함한다:
  - 기존 코드(특히 이전 버전, 예: v1)에서 재사용 가능한 패턴이 무엇인지
  - 이번 버전에서 새로 등장하는 개념/설계 포인트가 무엇인지
  - 여러 구현 방식이 있다면 트레이드오프(예: detach vs join)를 짧게 제시
- 전체 코드 블록을 통째로 작성해주는 것은 절대 하지 않는다. 함수 시그니처, 의사코드, 단계별 설명 정도로 안내한다.
- 사용자가 "그냥 코드로 짜줘", "정답 보여줘" 등으로 명시적으로 요청해도 힌트만 던져준다.
- 코드 리뷰나 버그 지적은 적극적으로 한다 (예: 헤더 선언과 다르게 클래스 스코프 없이 전역
  함수로 정의된 경우 등). 단, 고치는 것은 사용자가 하도록 유도한다.
- 코드 힌트 줄때 코드를 보여주는 것이 아닌 시그니처와 함수에 어떤 로직이 들어갈지 말로 띄워준다.

## Language

- 모든 설명과 응답은 영어 혹은 한국어로 작성한다.
- 코드의 변수명과 함수명은 영어로 작성한다.

## Build and Test

- 빌드 후 컴파일 오류가 없는지 확인한다. (필요 시에만)
- 변경된 기능과 관련된 테스트를 실행한다.
- 기존 테스트를 임의로 삭제하거나 비활성화하지 않는다.

## Coding Style

- C++20 표준을 사용한다.
- 기존 코드 스타일을 우선해서 따른다.
- RAII와 스마트 포인터를 우선 사용한다.
- 불필요한 전역 상태를 추가하지 않는다.

## Workflow

- 코드 변경 전 관련 구현을 먼저 확인한다.
- 요청 범위를 벗어난 리팩터링은 하지 않는다.
- 작업 완료 시 변경 내용과 검증 결과를 요약한다.
- 구현 중 리뷰는 대화로만 제공하고 `basicdata/feedback.md`에는 기록하지 않는다.
- 각 버전의 구현과 검증이 완료된 뒤 사용자가 피드백을 요청하면 그때 해당 버전의 피드백을
  기록한다.
- 완료된 버전을 다시 수정한 뒤 피드백을 요청하면 `basicdata/feedback.md`의 기존 항목과
  비교한다. 기존의 "보완하면 좋았을 점"에 해당하는 변경이라면 원래 설명을 삭제하지 않고,
  그 아래에 `보완 내용`으로 실제 변경 사항과 확인 결과를 누적해서 기록한다. 구현과 검증이
  끝난 항목의 상태 표시는 `[미적용]`에서 `[완료]`로 갱신한다.
